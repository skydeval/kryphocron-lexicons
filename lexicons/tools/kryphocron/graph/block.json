{
  "lexicon": 1,
  "id": "tools.kryphocron.graph.block",
  "description": "Private-tier block record. The blocker DID is the record's owner; the blocked DID is the subject. Block records are not federated and not publicly readable; existence is private information. Audience-list-exempt per §5.4 (consumed by BlockOracle outside the normal capability flow).",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["subject", "createdAt"],
        "properties": {
          "subject": {
            "type": "string",
            "format": "did",
            "description": "The blocked DID."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
